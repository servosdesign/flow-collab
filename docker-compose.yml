services:
  mongo:
    image: mongo:7
    container_name: vue-flow-sync-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: vue_flow_sync
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
